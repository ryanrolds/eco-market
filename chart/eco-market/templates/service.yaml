apiVersion: v1
kind: Service
metadata:
  name: {{ include "eco-market.fullname" . }}
  labels:
    {{- include "eco-market.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eco-market.selectorLabels" . | nindent 4 }}